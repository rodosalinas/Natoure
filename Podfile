# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Natoure' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire', '~> 4.7'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'FormTextField'
  pod 'FSCalendar'
  pod 'FacebookCore'
  pod 'FacebookLogin'
  pod 'FacebookShare'
  pod 'GoogleMaps'
  pod 'GooglePlaces'

  # Pods for Natoure

  target 'NatoureTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NatoureUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
